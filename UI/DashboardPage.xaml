<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Reframe.UI.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 + 引擎总开关 -->
        <StackPanel Orientation="Horizontal" Spacing="20" VerticalAlignment="Center">
            <TextBlock Text="仪表盘" FontSize="28" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <ToggleSwitch x:Name="EngineToggle" Header="引擎"
                          OnContent="运行中" OffContent="已停止"
                          Toggled="EngineToggle_Toggled"/>
        </StackPanel>

        <!-- 概览行 -->
        <TextBlock x:Name="SummaryText" Grid.Row="1" Opacity="0.7"/>

        <!-- 当前命中的窗口 -->
        <StackPanel Grid.Row="2" Spacing="8">
            <TextBlock Text="当前命中的窗口" FontWeight="SemiBold"/>
            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    CornerRadius="4" Padding="4" MinHeight="60">
                <ListView x:Name="HitList" SelectionMode="None" IsItemClickEnabled="False"
                          MaxHeight="160">
                    <ListView.Footer>
                        <TextBlock x:Name="HitEmpty" Text="暂无命中窗口" Opacity="0.5"
                                   Margin="8,4" Visibility="Collapsed"/>
                    </ListView.Footer>
                </ListView>
            </Border>
        </StackPanel>

        <!-- 日志流 -->
        <StackPanel Grid.Row="3" Spacing="8">
            <TextBlock Text="日志" FontWeight="SemiBold"/>
            <ListView x:Name="LogList" SelectionMode="None" IsItemClickEnabled="False"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                      CornerRadius="4"/>
        </StackPanel>
    </Grid>
</Page>
