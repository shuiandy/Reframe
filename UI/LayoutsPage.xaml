<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Reframe.UI.LayoutsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrl="using:Reframe.UI.Controls"
    xmlns:local="using:Reframe.UI"
    mc:Ignorable="d">

    <Grid Margin="24,16,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Uid="/LayoutsPage/Title" Style="{ThemeResource TitleTextBlockStyle}"/>

        <CommandBar Grid.Row="1"
                    DefaultLabelPosition="Right"
                    HorizontalAlignment="Left"
                    Background="Transparent">
            <AppBarButton x:Name="NewButton" x:Uid="/LayoutsPage/NewButton" Icon="Add" Click="NewButton_Click"/>
            <AppBarButton x:Name="DuplicateButton" x:Uid="/LayoutsPage/DuplicateButton" Icon="Copy" Click="DuplicateButton_Click"/>
            <AppBarButton x:Name="EditButton" x:Uid="/LayoutsPage/EditButton" Icon="Edit" Click="EditButton_Click"/>
            <AppBarButton x:Name="DeleteButton" x:Uid="/LayoutsPage/DeleteButton" Icon="Delete" Click="DeleteButton_Click"/>
        </CommandBar>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <Grid>
                <GridView x:Name="LayoutsGrid"
                          SelectionMode="Single"
                          IsItemClickEnabled="False"
                          DoubleTapped="LayoutsGrid_DoubleTapped"
                          SelectionChanged="LayoutsGrid_SelectionChanged">
                    <!-- Card hover is driven by the system GridViewItem container (no custom animation).
                         The container corner radius aligns with the inner card; pointer-over uses the
                         built-in visual states to swap the border/background brushes, which we override
                         locally to an accent outline plus a slightly lifted layer color. -->
                    <GridView.ItemContainerStyle>
                        <Style TargetType="GridViewItem">
                            <Setter Property="Margin" Value="4"/>
                            <Setter Property="CornerRadius" Value="8"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="UseSystemFocusVisuals" Value="True"/>
                        </Style>
                    </GridView.ItemContainerStyle>
                    <GridView.Resources>
                        <!-- pointer-over: border turns accent, background lifts slightly (both built-in ThemeResources) -->
                        <StaticResource x:Key="GridViewItemBackgroundPointerOver" ResourceKey="SubtleFillColorSecondaryBrush"/>
                        <StaticResource x:Key="GridViewItemBorderBrushPointerOver" ResourceKey="AccentFillColorDefaultBrush"/>
                        <!-- Selected state also uses an accent outline, echoing the hover state -->
                        <StaticResource x:Key="GridViewItemBorderBrushSelected" ResourceKey="AccentFillColorDefaultBrush"/>
                        <StaticResource x:Key="GridViewItemBorderBrushSelectedPointerOver" ResourceKey="AccentFillColorDefaultBrush"/>
                    </GridView.Resources>
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="local:LayoutItem">
                            <!-- The card background is painted with the card color; the hover lift comes
                                 from the outer container's border/background. The inner card border is
                                 transparent so the container's accent outline is the only edge (no double line). -->
                            <Grid Width="220" Height="180" Padding="10" RowSpacing="6"
                                  Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                  CornerRadius="7"
                                  ContextRequested="LayoutCard_ContextRequested">
                                <Grid.ContextFlyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem x:Uid="/LayoutsPage/MenuEdit" Click="EditButton_Click">
                                            <MenuFlyoutItem.Icon>
                                                <SymbolIcon Symbol="Edit"/>
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutItem x:Uid="/LayoutsPage/MenuDuplicate" Click="DuplicateButton_Click">
                                            <MenuFlyoutItem.Icon>
                                                <SymbolIcon Symbol="Copy"/>
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutSeparator/>
                                        <MenuFlyoutItem x:Uid="/LayoutsPage/MenuDelete" Click="DeleteButton_Click">
                                            <MenuFlyoutItem.Icon>
                                                <SymbolIcon Symbol="Delete"/>
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </Grid.ContextFlyout>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <ctrl:LayoutPreview Grid.Row="0" Layout="{x:Bind Model}"/>
                                <TextBlock Grid.Row="1"
                                           Text="{x:Bind Model.Name}"
                                           Style="{ThemeResource BodyStrongTextBlockStyle}"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Grid.Row="2"
                                           Text="{x:Bind SummaryText}"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>

                <StackPanel x:Name="EmptyHint"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Spacing="8"
                            Visibility="Collapsed">
                    <FontIcon Glyph="&#xE80A;" FontSize="40"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Uid="/LayoutsPage/EmptyHintText"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
